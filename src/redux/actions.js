import { createAction } from '@reduxjs/toolkit';

export const filter = createAction('FILTER');

export const changeThemeMode = createAction('CHANGE_THEME_MODE');

export const changeLocale = createAction('CHANGE_LOCALE');
